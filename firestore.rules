rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access to any document in the "users" collection
    // Each user has their own document identified by their secret key
    match /users/{userId} {
      allow read, write: if true;
    }
  }
}
